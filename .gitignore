.venv/

# Editor and OS files
.vscode/
.DS_Store

# AI assistant config files
.claude/
.github/copilot-instructions.md
CLAUDE.md

# Python cache/build artifacts
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/

# Experiment outputs and logs
runs/
slurm_jobs/
results_*/
predictions_*/

# Artifacts — ignore bulk experiment data, but track pre-trained models
artifacts/*
!artifacts/models/
artifacts/models/*
!artifacts/models/mlp/
!artifacts/models/random/
!artifacts/packages/

# Random model binaries: included in release branch (pre-built for CPU inference).
# On master they are excluded (reproducible via retrain_random_models.py).

# TensorBoard/event logs (inside tracked artifact dirs too)
events.out.tfevents.*

# Thesis description (local only)
Ausschreibung.pdf
